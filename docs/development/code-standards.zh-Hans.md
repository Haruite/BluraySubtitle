# 代码修改规范

[English](code-standards.md)

## 1. 权威来源与适用范围

本规范是所有向本仓库提交 PR 或代码修改的贡献者必须遵守的强制要求。

规则权威顺序如下：

1. 本规范；
2. `README.md` 和 `README.zh-Hans.md` 描述的产品行为；
3. 不与以上内容冲突的实现细节。

代码可以包含比 README 更详细的实现，但不能与 README 冲突。记录已确认的产品行为时，必须同步更新中英文 README。

作者确定新规则后，必须在依赖该规则的代码修改之前或同一次修改中同步更新本文件及英文版本。重构或重大改动还必须同步更新中英文[重构历史](../refactoring/refactoring-history.zh-Hans.md)；一般修改不需要增加重构历史记录。

## 2. 简单与一致

- 使用能够正确解决问题的最简单实现。
- 在当前修改范围内删除重复、冲突、不可达和不必要的逻辑。
- 不为产品并不需要的情况增加推测性校验或抽象。
- 避免任何与目的无关的不必要校验。优先使用简单的结构或字面量检查，只有格式本身属于需求时才使用严格的正则表达式匹配。
- 在拥有该事实的边界校验一次，不要让每一层重复相同校验。
- 真正共用的行为只保留一份实现；不能仅因为代码看起来相似，就强迫无关工作流共用抽象。
- 一个函数完成一个完整操作。不要把直接的操作拆成大量只转发参数或只有一行的短函数。
- 在不制造无关巨型函数的前提下，尽量减少函数数量。
- 名称应表达领域值或操作。除惯用且范围很小的索引外，不使用无意义临时名称。
- 导入、类型标注、异常处理和格式应与所在模块保持一致。
- 除非仓库内部调用方或已确认需求需要，否则不保留兼容包装层或 facade API。
- 宽泛异常处理不能隐藏无效配置或执行失败。只有在明确的 GUI/Worker 边界或尽力清理边界，并且失败仍得到恰当处理时才可使用。

## 3. 源码语言、注释与命名

- Python 源码字符串一律使用英文；`I18N_ZH_TO_EN` 中作为目录键使用的中文是特定例外。
- 代码注释和 docstring 一律使用英文。
- 对关键领域规则、不直观的计算、所有权边界和有意保留的例外补充注释。
- 不添加只是重复显而易见代码含义的注释。
- 变量、函数、类和字段使用有实际含义的名称。

## 4. GUI 是执行契约

- 当前 GUI 可见状态具有最高权威：已选行、顺序、路径、名称、语言、模式、命令、章节范围、编码器及其他选项都必须准确应用。
- 任务启动时一次性捕获 GUI 状态，并尽可能通过一个显式 Request 传递。
- Worker 或 Service 不能把显式 GUI 值静默替换为旧快照、全局值、默认值、重新生成值或推测值。
- 不能静默跳过 GUI 中已选择的行或选项。无法执行时必须报错。
- 只有用户没有提供显式值时才能自动推断。
- 除非 GUI 明确说明其他排序规则，执行顺序必须与启动时捕获的可见表格顺序一致。
- Worker 拥有已经捕获的 Request，启动后不能再读取实时 GUI 控件。
- Service 只消费普通 Python 数据，不能读取或重新解释 Qt 表格和控件。
- 长时间任务必须离开 GUI 线程执行，并保持进度、取消、成功和错误处理一致。

## 5. 预检查与失败处理

- 可预知且可操作的问题尽可能提前检查，优先在 Worker 启动前检查。
- 预检查应集中于 Request 中已经确定的事实：必需路径、已选输入、所需工具、无效区间、不完整行映射、命令结构、输出路径和可预知冲突。
- 不重复执行昂贵的媒体探测，也不在没有已确认规则时增加会拒绝有效输入的苛刻校验。
- 执行时才发现的媒体相关失败必须指出受影响的源文件或行，并明确报错。
- 计划输出已存在时默认报错；只有已确认的产品规则明确将工作流定义为可恢复时才可例外。已有输出不能被覆盖、改名，也不能被复用为其他结果。
- 可恢复工作流必须记录哪些输出类型可视为已完成，并在任务进度中报告每一个跳过的输出。
- 能在执行前确定输出路径时，必须在第一次写入前计算并检查完整输出集合。
- 能在执行前完成映射规划时，必须先完成规划，再修改源文件或创建最终输出。
- 外部命令必须检查返回状态。只有已经记录或验证的工具警告返回码才可以视为成功。
- 外部工具优先使用参数列表和 `shell=False`；只有确实需要 shell 语法时才能使用 shell。
- 清理操作只能删除当前任务创建的临时文件或不完整产物，不能删除用户原本存在的文件。
- 对长时间运行的 Encode 批处理，可确定的请求级数据安全问题仍须在 Worker 启动前阻止执行。开始逐行执行后，仅影响单行的失败必须记录，并且不能阻止后续行继续；用户取消或请求级不安全状态仍须停止整批任务。
- Encode 行失败时，由该行创建且非空的编码基本流、部分容器以及提取或注入的动态元数据产物必须使用唯一的非正式文件名保留，并在错误报告中列出。只有该行最终输出成功后才能删除这些产物。
- 长时间 Encode 批处理运行期间不能显示模态错误窗口。必须汇总各行警告和失败，并在 Worker 清理完成后只显示一次结果摘要。

## 6. 分层职责

- GUI/配置层：一次性读取当前控件，规范化显式值，执行确定性预检查并创建完整 Request。
- Worker 层：拥有一个 Request、进度回调、取消状态及最终成功/错误信号。
- Service 层：根据普通数据执行工作流和领域逻辑，不查询 GUI 或隐藏全局状态。
- 领域/工具层：执行可复用的媒体计算和显式外部工具操作。
- 避免可变的模块级工作流配置。
- 不要把同一个配置既写入 Service 属性，又作为方法参数重复传递。
- 只共享领域计算和写入原语，不共享过期的工作流状态。
- `src/runtime/gui_runtime_split` 中 mixin 实现的所有方法，必须在 `gui_base.py` 中有签名一致的声明。
- `src/runtime/services_split` 中 mixin 实现的所有方法，必须在 `service_base.py` 中有签名一致的声明。

## 7. i18n 与用户可见文本

- 每个 GUI 字符串、对话框字符串、进度文本、终端信息和其他用户可见输出都必须提供英文和简体中文版本。
- 生产代码使用英文作为源字符串。
- 在同一次修改中把英文映射加入 `src/core/i18n.py:I18N_ZH_TO_EN`。
- 在合适的展示边界通过 `self.t(...)` 或 `translate_text(...)` 输出用户可见文本。
- 动态信息应翻译稳定模板，再填入具体值。
- 记录产品行为或与用户有关的使用注意事项时，必须同时更新 `README.md` 和 `README.zh-Hans.md`。
- 除专门的“实现说明”或“实现细节”段落与章节外，README 不得介绍程序实现细节。README 应尽量简洁，只保留当前功能、使用注意事项和其他对用户有效的信息。历史行为对比、已删除行为、重构原因和未来清理计划应写入重构历史或开发文档。

## 8. 文件格式

- 源码和文档使用 UTF-8。
- 所有新增或修改的非 `.sh` 文本文件必须使用 CRLF。
- shell 脚本必须使用 LF，避免 shebang 失效。
- 不引入行尾空格或错误编码。

## 9. 工具版本与 Dockerfile 维护

- 如果不存在已确认的兼容性问题或其他技术约束，依赖与随附工具必须使用官方上游发布的最新版本；没有此类约束时，不得固定版本或提交。
- `Dockerfile` 是 `setup_linux_environment.sh` 面向 Ubuntu 26.04 的适配版，不需要考虑其他操作系统兼容性，也不需要增加说明性输出或注释。
- Linux setup 必须把受管理的可执行文件和 VapourSynth 插件放到 `src/core/settings.py` 定义的 Linux 路径。Docker 必须在各工具原有构建段内直接安装到该文件中对应的 Docker 路径，不得增加末尾统一搬运层。
- 修改已有软件时，必须修改 Dockerfile 中对应的原有构建段，即使因此使后续层缓存失效也不得规避。不得把无关的小改动放到文件前部；确实新增的软件应在可行时尽量放到末尾，以保留前面构建层的缓存。

## 10. 已确认的产品约束

- Remux 来源 Encode 支持恢复执行。已有的计划正片、SP、外挂字幕和附带文件输出视为已完成并跳过且不覆盖，其余行继续执行；当前请求内部的重复路径仍然报错。
- 原盘 DIY 保持可见并保留代码；未完成的执行逻辑不能表现为已经完成。
- 原盘 Remux 提供默认启用的选项，在正片和 SP 混流完成后将已选择的无损音轨转换为 FLAC；关闭时保留源音轨，Remux 的这项转换不能使用 AAC 或 Opus。
- DTS 系列音轨成功转换为 FLAC 后，仅当 FLAC 不大于提取出的 DTS 时才替换源音轨；否则丢弃 FLAC 并保留原 DTS。PCM 和 TrueHD/MLP 成功转换出的 FLAC 不受体积限制。
- 最终 Remux 和 Encode 输出会自动移除解码后最大音量低于 -60 dB 的已选音轨，以及源编码家族和声道数相同的完全相同解码音轨。已知语言不同的音轨不能互相去重，重复时保留源顺序中最早的一条，并报告每次移除。该文档化清理规则是“保留所有已选轨道”的明确例外。
- 原盘 Encode 的暂存 Remux 必须保留源音轨；只有视频压制成功后，才在最终混流阶段执行 Encode 音频转换。
- 启用 Encode 对比图时，每个压制视频必须在实际的逐来源输出目录下的 `Compare` 中保存一组源／成品 PNG，两张图必须使用相同的从零开始视频帧号；按时间点匹配不等价。
- 原盘 Remux 和原盘 Encode 不支持通用视频转换；未来 DIY 视频转换需要单独确认设计。
- 每个已选择的主 MPLS 必须且只能对应一条非空主 Remux 命令。
- 添加章节按照当前可见 MKV 顺序与所选主播放列表依次匹配，不要求外部 MKV 文件名包含 `BD_Vol_NNN`。
- 应用偏好使用可执行文件旁带版本号的 `config.json`；源码运行时使用仓库根目录。冻结版本必须打包 `config.default.json` 作为首次运行模板，打包目录 `_MEIPASS` 不能作为可写配置目标。
- 更新检查必须由用户手动触发并异步执行，只能查询 GitHub 最新正式 Release 的版本标签，并与程序自身版本号进行数值比较，不得下载更新。发现新版本时必须给出 GitHub Releases 链接，并明确提醒用户把当前程序目录中的 `config.json` 复制到新程序目录。
- 无效应用配置必须明确报错并保留原文件。配置写入必须以原子方式替换完整文件。
- 配置的默认值只用于初始化可见 GUI 控件。程序运行期间必须保留用户的修改，启动任务时使用当前可见值，不能重新读取配置默认值替换这些值。
- Encode 内置预设保持只读并继续定义在 `src/core/encode_presets.py`；应用配置只保存按单一编码器归属的用户新增预设。Encode 页面只显示当前编码器的内置和用户预设，直接编辑当前可见参数框时不能改变所选预设名称。
- FDK-AAC 和 Opus 码率值 `0` 表示自动行为：FDK-AAC 使用 VBR 模式 5；Opus 对不超过双声道使用 128 kbps，对更多声道使用 256 kbps。正数表示明确的 kbps 目标。
- “高级”设置页必须明确提示较高的 FFmpeg FLAC 压缩等级会显著增加 Remux 时间；同时必须显示立体声音乐的起始建议范围：FDK-AAC 128–256 kbps、Opus 64–128 kbps，并提示多声道或混合声道布局使用“自动”或更高的值。
- 正常关闭时自动保存窗口位置和大小以及当前语言、主题、字号和透明度。成功恢复的窗口位置不能再被首次启动居中逻辑覆盖。

## 11. 测试与修改报告

- 每个修改过的工作流边界和可确定测试的已修复回归都要补充针对性自动测试。
- 一个工作流或阶段完成时执行仓库集中测试。
- 根据修改范围，至少从以下检查中执行适用项目：
  - Python 编译和导入冒烟测试；
  - 仓库单元测试；
  - `tools/check_i18n.py`；
  - `tools/check_split_contracts.py`；
  - `git diff --check`；
  - 新增和修改文件的 CRLF/LF 检查。
- 完整蓝光和 MKV 素材属于手动回归输入，不是 CI 固件。必须准确说明仍需执行哪些真实素材测试、会写入什么，以及是否必须使用可丢弃副本。
- 每批修改结束时必须报告：
  - 修改的文件和区域；
  - 删除的冗余或冲突路径；
  - 每个业务逻辑变化，包括旧行为和新行为；
  - 已执行测试及结果；
  - 仍需完成的真实素材检查；
  - README、i18n 和规范更新；属于重构或重大改动时还要报告重构历史更新。

## 修改完成检查表

- [ ] 当前 GUI 值原样传到运行时。
- [ ] 没有已选择的值被静默跳过或替换。
- [ ] 可预知失败和输出冲突已尽早检查。
- [ ] 已存在文件不会被隐式覆盖。
- [ ] 代码字符串和注释均为英文。
- [ ] 每个用户可见字符串都有英文/简体中文 i18n。
- [ ] GUI/Service split 基类声明已经同步。
- [ ] 新增和修改文件使用规定的换行符。
- [ ] 已按修改范围执行针对性和集中测试。
- [ ] 行为变化已同步到中英文 README。
- [ ] 新确认规则已同步到中英文规范。
- [ ] 属于重构或重大改动时，已同步更新中英文重构历史。
